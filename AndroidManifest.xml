<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="dime.android.todo" android:versionCode="5"
	android:versionName="0.5.1">
	<uses-sdk android:minSdkVersion="4" />
	<supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:anyDensity="true" />

	<application android:name="ToDo" android:icon="@drawable/icon"
		android:label="@string/app_name" android:theme="@style/MyTheme">
		<activity android:name=".ui.ToDoList" android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		
		<activity android:name=".ui.Preferences"></activity>

		<activity android:name=".ui.NewEditToDo" android:label="@string/new_edit_todo"
			android:theme="@style/MyTheme" android:windowSoftInputMode="stateVisible|adjustResize" />
		
		<!-- Broadcast Receiver that will process AppWidget updates -->
		<receiver android:name=".widget.ToDoWidget" android:label="@string/widget_name">
			<intent-filter>
				<action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
			</intent-filter>
			<meta-data android:name="android.appwidget.provider"
				android:resource="@xml/todo_widget" />
		</receiver>

		<!-- Service to perform web API queries -->
		<service android:name=".widget.ToDoWidgetService" />

	</application>
</manifest>